// Public, non-secret runtime configuration.
// Keep OOS disabled until a reviewed gateway contract and endpoint exist.
window.OOS_CONFIG = Object.freeze({
  enabled: false,
  gatewayUrl: "",
  timeoutMs: 1800
});
